package com.bronx.telegram.notification.model.enumz;

public enum RegistrationState {
    IDLE,
    AWAITING_EMAIL,
    AWAITING_EMP_CODE,
    AWAITING_FULL_NAME,
    AWAITING_ROLE,
    AWAITING_CONTACT,
    AWAITING_FINISH
}
